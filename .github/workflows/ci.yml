name: CI Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build server and client
        env:
          MONGO_URI: ${{ secrets.MONGO_URI }}
        run: npm run build-all

      - name: Start server in background
        env:
          MONGO_URI: ${{ secrets.MONGO_URI }}
          PORT: 5000
        run: |
          echo "Starting built server..."
          nohup node server/dist/server.js > server.log 2>&1 &

      - name: Show startup log
        run: sleep 10 && cat server.log || true

      - name: Run Cypress tests
        env:
          MONGO_URI: ${{ secrets.MONGO_URI }}
          PORT: 5000
        run: npm run cy:run

      - name: Show server log if failure
        if: failure()
        run: cat server.log
