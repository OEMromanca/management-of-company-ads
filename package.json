{
  "name": "advertisement-management-ts-app",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server",
    "scripts"
  ],
  "scripts": {
    "start-client": "npm --workspace client run dev",
    "build-client": "npm --workspace client run build",
    "start-server": "npm --workspace server run dev",
    "build-server": "npm --workspace server run build",
    "convert-font": "ts-node scripts/convert-font.cjs",
    "dev": "concurrently \"npm run start-client\" \"npm run start-server\"",
    "build-all": "npm run build-server && npm run build-client",
    "test:cy:chrome": "start-server-and-test start-server http://localhost:5000 120000 'npx cypress run --browser chrome'",
    "test:cy:firefox": "start-server-and-test start-server http://localhost:5000 120000 'npx cypress run --browser firefox'",
    "test:cy:electron": "start-server-and-test start-server http://localhost:5000 120000 'npx cypress run --browser electron'"
  },
  "devDependencies": {
    "@types/pdfmake": "^0.2.11",
    "concurrently": "^9.2.1",
    "start-server-and-test": "^2.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2",
    "wait-on": "^9.0.1"
  },
  "dependencies": {
    "pdfmake": "^0.2.20",
    "typeface-roboto": "^1.1.13"
  }
}
